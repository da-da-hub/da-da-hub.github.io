source "https://rubygems.org"

# Replace github-pages with the standard jekyll gem
gem "jekyll", "~> 4.3" # or your preferred version

# Explicitly list the plugins you actually use
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
  # Add other plugins here
end
